CREATE TABLE Agenda (
     codigo     INT AUTO_INCREMENT  NOT NULL,
     nome       VARCHAR(50)         NULL,
     telefone   VARCHAR(10)         NULL,
     dataRagistro      DATE         NULL,
     PRIMARY KEY (codigo)
);